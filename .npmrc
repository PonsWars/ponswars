# Surface phantom dependencies instead of hiding them behind hoisting.
shamefully-hoist=false
strict-peer-dependencies=false
# Every workspace dependency must be declared; no implicit resolution.
auto-install-peers=true
