# Dependencies
node_modules/
.pnpm-store/

# Build output
dist/
build/
out/
.turbo/
*.tsbuildinfo

# Test / coverage
coverage/
.vitest/

# Environment — never commit real credentials
.env
.env.*
!.env.example

# Contracts (Foundry)
# lib/ is deliberately NOT ignored: forge-std is a git submodule, and the
# pinned commit is part of what makes a build reproducible.
contracts/out/
contracts/cache/
contracts/broadcast/

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Editors / OS
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea/
.DS_Store
Thumbs.db

# Local scratch
tmp/
.scratch/

# Byte-identical duplicate of the masterplan, kept locally only
_archive/

# Local scratch used while deriving golden values
.scratch/

# Vite build output
apps/web/dist/
