{
  "name": "@ponswars/battle-engine",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "The deterministic battle engine core. Same evidence bundle, same result — always.",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean"
  },
  "dependencies": {
    "@ponswars/battle-math": "workspace:*",
    "@ponswars/shared-types": "workspace:*"
  }
}
