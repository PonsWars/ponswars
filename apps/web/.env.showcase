# The web client with no server behind it: every surface in its preview state,
# under the preview banner. `vite --mode showcase` (see .claude/launch.json).
# Nothing secret can live here — both values are empty on purpose.
VITE_API_URL=
VITE_WS_URL=
